server:
  port: 9300
spring:
  application:
    name: consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka
  instance:
    instance-id: mycloud-consumer1
    prefer-ip-address: true
feign:
  circuitbreaker:
    enabled: true
